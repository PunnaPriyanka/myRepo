pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                git 'https://github.com/PunnaPriyanka/myRepo.git'
            }
        }
        stage('Publish') {
           steps{
               publishHTML([
                 allowMissing:true,
                 alwaysLinkToLastBuild : false,
                 KeepAll : false,
                 reportDir : '.',
                 reportFiles : 'simple.html',
                 reportName : 'MY HTML PAGE'
               ])
           }
            
        }
    }
}
